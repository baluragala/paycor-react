//var fullName = "N/A";

// function makeName(first, last) {
//   var fullName;
//   if (first.length > 3) {
//     fullName = first + "," + last;
//     return fullName;
//   }
//   return fullName;
// }

var fullName = "N/A";

function makeName(first, last) {
  if (first.length > 3) {
    let fullName = first + "," + last;
    return fullName;
  }
  return fullName;
}

const PI = 3.414;
let a3 = 20;
